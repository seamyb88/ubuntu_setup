sudo apt install zsh
sudo chsh -s $(which zsh) $USER
sh -c "$(curl -fsSL https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
Tricolour prompt is changed in the zsh theme (ohmyzsh) which is currently fino
